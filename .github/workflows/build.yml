name: Oasis Final Build
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          # حل تعارضات npm لضمان تثبيت حزم Zego و Expo بنجاح لضمان الاتصال الحقيقي
          echo "legacy-peer-deps=true" > .npmrc
          
          # [حل جذري لخطأ الصور 1000079122 و 1000079172] تعريف الـ SDK يدوياً
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp",
                "compileSdkVersion": 34,
                "targetSdkVersion": 34,
                "buildToolsVersion": "34.0.0",
                "permissions": ["CAMERA", "RECORD_AUDIO", "POST_NOTIFICATIONS"]
              },
              "extra": {
                "eas": { "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53" }
              }
            }
          }' > app.json
          
          npm pkg delete type
          
          # تثبيت أدوات بناء Expo والمكتبات الأساسية (لحل أخطاء الصور 1000079060)
          npm install @expo/config-plugins@~8.0.0 @expo/prebuild-config expo-modules-core@~1.12.0 --save --legacy-peer-deps
          npm install expo-notifications expo-device expo-constants crypto-js react-delegate-component --save --legacy-peer-deps
          npm install @zegocloud/zego-uikit-prebuilt-call-rn zego-zim-react-native zego-zpns-react-native --save --legacy-peer-deps
          
          npm install --legacy-peer-deps
          
          # [إصلاح جراحي حاسم]: حقن الـ Namespace لمنع تعارضات Gradle 8.3 الموضحة في السجلات
          find node_modules -name "*.gradle" -exec sed -i '/android {/a \    namespace "com.oasis.app"' {} + || true

          # [إصلاح الأخطاء الموضحة في الصور 1000079060 و 1000079122]
          # تصحيح مراجع Kotlin (extensions و extra) وإجبار استخدام SDK 34 في كل المكتبات
          find node_modules -name "*.gradle" -exec sed -i 's/compileSdkVersion .*/compileSdkVersion 34/g' {} + || true
          find node_modules -name "*.gradle" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "*.gradle" -exec sed -i 's/extra\[/project.ext\[/g' {} + || true
          find node_modules -name "*.kts" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          
          # إصلاح تعارضات ZegoCloud في ملفات تعريف الأندرويد لضمان استقرار الاتصال
          find node_modules -name "AndroidManifest.xml" -exec sed -i 's/package="com.zegouikitrebuiltcallrn"/ /g' {} + || true
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK (Final Fix & Clear Cache)
        run: |
          cd mobile
          export NPM_CONFIG_LEGACY_PEER_DEPS=true
          # استخدام --clear-cache لمسح بقايا المحاولات الفاشلة الموضحة في الصور وضمان بناء نظيف تماماً
          npx eas-cli build --platform android --profile preview --non-interactive --clear-cache
