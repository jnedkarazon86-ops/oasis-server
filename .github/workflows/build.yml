name: Oasis Auto Build APK
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Prep & Clean Build
        run: |
          cd mobile
          # إزالة أي آثار لـ module قد تسبب خطأ الـ import
          npm pkg delete type
          npm install --legacy-peer-deps
          # تطبيق الإصلاح الجراحي لـ ZegoCloud
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK
        run: |
          cd mobile
          # استخدام أمر البناء مع تجاهل أخطاء الـ config المزعجة
          npx eas-cli build -p android --profile preview --non-interactive --no-wait
