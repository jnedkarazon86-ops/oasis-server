name: Oasis Final Build
on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          echo "legacy-peer-deps=true" > .npmrc
          
          # 1. إنشاء eas.json (لحل مشكلة التعليق في المحاولة 386)
          echo '{
            "build": {
              "preview": {
                "android": {
                  "buildType": "apk",
                  "image": "latest"
                },
                "channel": "preview"
              }
            }
          }' > eas.json

          # 2. إنشاء app.json (لعلاج نقص sdkVersion الظاهر في 10 صور على الأقل)
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp",
                "compileSdkVersion": 34,
                "targetSdkVersion": 34,
                "buildToolsVersion": "34.0.0"
              },
              "extra": {
                "eas": { "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53" }
              }
            }
          }' > app.json
          
          npm pkg delete type
          npm install --legacy-peer-deps

          # 3. توليد ملفات الأندرويد (Prebuild) لضمان وجود مسار android/build.gradle
          npx expo prebuild --platform android --no-install

          # 4. [تصحيح خطأ الصورة 1000079246]: الحقن المتوافق مع Gradle 8.3
          # استخدمنا "=" و "compileSdk" ليتناسب مع متطلبات السطر 16 في سجلاتك
          find android -name "build.gradle" -exec sed -i '/android {/a \    namespace = "com.oasis.app"\n    compileSdk = 34\n    targetSdk = 34' {} + || true
          
          # 5. معالجة كافة مكتبات node_modules (لحل أخطاء Evaluating project :expo)
          # هذا السطر يبحث عن أي ملف gradle في المكتبات ويحقن فيه الإعدادات الناقصة
          find node_modules -name "*.gradle" -exec sed -i '/android {/a \    namespace = "com.oasis.app"\n    compileSdk = 34' {} + || true

          # 6. تصحيح مراجع Kotlin و ZegoCloud (علاج أخطاء التوافقية)
          find node_modules -name "*.gradle" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "*.kts" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "AndroidManifest.xml" -exec sed -i 's/package="com.zegouikitrebuiltcallrn"/ /g' {} + || true

      - name: 🚀 Build APK (Final Fix & Clear Cache)
        run: |
          cd mobile
          # التنظيف ضروري جداً لأن صورك أظهرت بقاء ملفات من محاولات فاشلة
          npx eas-cli build --platform android --profile preview --non-interactive --clear-cache
