name: Oasis Auto Build APK
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Prep & Emergency Fix
        run: |
          cd mobile
          # إزالة أي إعدادات تسبب تعارض نظام الوحدات
          npm pkg delete type
          # الخطوة القاضية: تحويل ملف الإعدادات لـ JSON لضمان القراءة
          npx expo config --json > app.json 2>/dev/null || true
          npm install --legacy-peer-deps
          # إصلاح ZegoCloud
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK
        run: |
          cd mobile
          # تشغيل البناء مع تجاهل التحقق من الـ Config لأنه جاهز كـ JSON
          npx eas-cli build -p android --profile preview --non-interactive
