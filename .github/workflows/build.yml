name: Oasis App Final Success Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 1. Checkout Code
        uses: actions/checkout@v4

      - name: 2. Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 3. Setup EAS CLI
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 4. Installation & Patching
        # التوجه مباشرة لمجلد mobile بناءً على الصورة
        working-directory: oasis-server/mobile
        run: |
          # تنظيف الكاش لمنع أخطاء الذاكرة (Buffer) الظاهرة في السجلات
          npm cache clean --force
          rm -rf node_modules package-lock.json
          
          # تثبيت الحزم (سيقوم patch-package بتطبيق ملف التصحيح آلياً)
          npm install --legacy-peer-deps --force
          
          # مشرط الطوارئ (sed): حذف typeof يدوياً وبسرعة البرق لضمان نظافة الكود تماماً
          echo "Applying emergency cleaning for @zegocloud..."
          find node_modules/@zegocloud -name "*.js" -type f -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} +

      - name: 5. Launch EAS Cloud Build
        working-directory: oasis-server/mobile
        run: |
          # فحص أمان أخير للتأكد من اختفاء كلمة typeof المسببة للخطأ
          grep -r "typeof " node_modules/@zegocloud || echo "✅ Success: Code is Clean!"
          
          # إطلاق البناء السحابي النهائي
          eas build --platform android --profile preview --non-interactive --no-wait --clear-cache
