name: Oasis Final Build
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          # حل مشكلة التعارضات ومنع توقف البناء بسبب الحماية الصارمة لـ npm
          echo "legacy-peer-deps=true" > .npmrc
          
          # تأمين هوية التطبيق وتحديث الإعدادات لتشمل التراخيص المطلوبة في App.js
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp",
                "permissions": ["CAMERA", "RECORD_AUDIO", "POST_NOTIFICATIONS"]
              },
              "extra": {
                "eas": {
                  "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53"
                }
              }
            }
          }' > app.json
          
          npm pkg delete type
          
          # [تحديث هام] تثبيت المكتبات الجديدة التي أضفتها في App.js لضمان عمل الإشعارات والاتصال
          npm install expo-notifications expo-device expo-constants crypto-js react-delegate-component --save --legacy-peer-deps
          npm install @zegocloud/zego-uikit-prebuilt-call-rn zego-zim-react-native zego-zpns-react-native --save --legacy-peer-deps
          
          # تثبيت بقية تبعيات المشروع
          npm install --legacy-peer-deps
          
          # إصلاح تعارض الأسماء في أندرويد لضمان عبور مرحلة التجميع النهائي (Gradle)
          find node_modules -name "AndroidManifest.xml" -exec sed -i 's/package="com.zegouikitrebuiltcallrn"/ /g' {} + || true
          
          # إصلاحات الأخطاء البرمجية داخل مكتبات Zego لضمان التوافق مع React Native الحديث
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK (Final Fix & Clear Cache)
        run: |
          cd mobile
          # إجبار السيرفر على تجاهل تعارضات الإصدارات أثناء التجميع
          export NPM_CONFIG_LEGACY_PEER_DEPS=true
          # استخدام --clear-cache لمسح أي مخلفات من المحاولات الـ 200 الفاشلة السابقة
          npx eas-cli build --platform android --profile preview --non-interactive --clear-cache
