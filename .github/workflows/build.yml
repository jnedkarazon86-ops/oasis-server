name: Oasis Final Build
on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          echo "legacy-peer-deps=true" > .npmrc
          
          # [تعديل eas.json] لفرض بيئة بناء تدعم SDK 34 وتتجاوز أخطاء الصور 1000079138 و 1000079200
          echo '{
            "build": {
              "preview": {
                "android": {
                  "buildType": "apk",
                  "image": "ubuntu-22.04-jdk-17-ndk-r25b"
                }
              }
            }
          }' > eas.json

          # إعداد app.json لتعريف الـ SDK يدوياً (علاج خطأ compileSdkVersion is not specified)
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp",
                "compileSdkVersion": 34,
                "targetSdkVersion": 34,
                "buildToolsVersion": "34.0.0"
              },
              "extra": {
                "eas": { "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53" }
              }
            }
          }' > app.json
          
          npm pkg delete type
          npm install --legacy-peer-deps
          
          # [إصلاح حاسم]: حقن الـ Namespace والـ SDK 34 مباشرة في مكتبات node_modules
          # هذا يمنع خطأ "A problem occurred evaluating project :expo" المكرر في صورك
          find node_modules -name "*.gradle" -exec sed -i '/android {/a \    namespace "com.oasis.app"\n    compileSdkVersion 34' {} + || true

          # تصحيح مراجع Kotlin و ZegoCloud لضمان استقرار الاتصال
          find node_modules -name "*.gradle" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "*.kts" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "AndroidManifest.xml" -exec sed -i 's/package="com.zegouikitrebuiltcallrn"/ /g' {} + || true

      - name: 🚀 Build APK (Final Fix & Clear Cache)
        run: |
          cd mobile
          # استخدام --clear-cache لمسح بقايا المحاولات الفاشلة الموضحة في صورك السابقة
          npx eas-cli build --platform android --profile preview --non-interactive --clear-cache
