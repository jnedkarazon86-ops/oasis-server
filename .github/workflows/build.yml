name: Oasis Final Build
on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          echo "legacy-peer-deps=true" > .npmrc
          
          # [إصلاح حاسم]: إضافة channel لضمان خروج الأمر وعدم تعليقه كما في الصورة 1000079226
          echo '{
            "build": {
              "preview": {
                "android": {
                  "buildType": "apk",
                  "image": "latest"
                },
                "channel": "preview"
              }
            }
          }' > eas.json

          # إعداد app.json (علاج خطأ compileSdkVersion الموضح في الصور)
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp",
                "compileSdkVersion": 34,
                "targetSdkVersion": 34,
                "buildToolsVersion": "34.0.0"
              },
              "extra": {
                "eas": { "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53" }
              }
            }
          }' > app.json
          
          npm pkg delete type
          npm install --legacy-peer-deps
          
          # حقن الـ Namespace والـ SDK 34 مباشرة في المكتبات (علاج جذري لصور الأخطاء)
          find node_modules -name "*.gradle" -exec sed -i '/android {/a \    namespace "com.oasis.app"\n    compileSdkVersion 34' {} + || true

          # تصحيح مراجع Kotlin و ZegoCloud
          find node_modules -name "*.gradle" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "*.kts" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "AndroidManifest.xml" -exec sed -i 's/package="com.zegouikitrebuiltcallrn"/ /g' {} + || true

      - name: 🚀 Build APK (Final Fix & Clear Cache)
        run: |
          cd mobile
          # استخدام --non-interactive لضمان عدم توقف الأمر بانتظار إدخال يدوي
          npx eas-cli build --platform android --profile preview --non-interactive --clear-cache
