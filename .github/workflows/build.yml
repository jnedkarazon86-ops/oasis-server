name: Oasis Final Build
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          # إنشاء app.json لضمان مطابقة الهوية com.aboahj.oasisapp مع المفتاح المرفوع
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp"
              },
              "extra": {
                "eas": {
                  "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53"
                }
              }
            }
          }' > app.json
          
          # إزالة قيود النوع وتثبيت التبعيات مع حل تعارض النسخ (ERESOLVE)
          npm pkg delete type
          npm install --legacy-peer-deps
          
          # إصلاح ZegoCloud لضمان فتح نفق الاتصال بنجاح
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK (Fixed Command)
        run: |
          cd mobile
          # إجبار الخادم السحابي على قبول التعارضات أثناء التجميع النهائي
          export NPM_CONFIG_LEGACY_PEER_DEPS=true
          # استخدام الأمر القياسي الصحيح لتجنب خطأ "الوسيط غير المتوقع"
          npx eas-cli build --platform android --profile preview --non-interactive
