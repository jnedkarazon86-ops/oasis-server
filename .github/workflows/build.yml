name: Oasis Final Build
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          # إنشاء app.json لضمان مطابقة Package Name مع Keystore
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp"
              },
              "extra": {
                "eas": {
                  "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53"
                }
              }
            }
          }' > app.json
          
          # مسح أي قيود قديمة وتثبيت التبعيات مع تجاهل التعارضات
          npm pkg delete type
          npm install --legacy-peer-deps
          
          # إصلاح ZegoCloud لضمان فتح نفق الاتصال
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK (Force Integration)
        run: |
          cd mobile
          # تفعيل خيار التجاهل التلقائي لتعارضات المكتبات أثناء البناء السحابي
          export NPM_CONFIG_LEGACY_PEER_DEPS=true
          # بناء التطبيق مع استخدام مفتاح التوقيع المرفوع مسبقاً
          npx eas-cli build -p android --profile preview --non-interactive --auto-submit-credentials
