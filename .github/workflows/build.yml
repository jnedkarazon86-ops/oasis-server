name: Oasis Auto Build APK
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: |
          cd mobile
          npm install --legacy-peer-deps

      - name: 🛠 Surgical Patch (Fix ZegoCloud Code)
        run: |
          cd mobile
          # هذا الأمر يبحث عن السطر المعطل ويحذفه برمجياً لضمان عدم فشل البناء
          sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" node_modules/@zegocloud/zego-uikit-rn/index.js || true
          sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" node_modules/@zegocloud/zego-uikit-prebuilt-call-rn/index.js || true

      - name: 🚀 Build APK
        run: |
          cd mobile
          npx eas-cli build -p android --profile preview --non-interactive
