name: Oasis Auto Build APK
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Clean Install & Zego Fix
        run: |
          cd mobile
          # إزالة أي ملفات إعدادات مؤقتة قديمة
          rm -f app.json
          npm pkg delete type
          npm install --legacy-peer-deps
          # إصلاح كود ZegoCloud ليتوافق مع أندرويد
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK
        run: |
          cd mobile
          # إجبار النظام على تجاهل أخطاء الـ Import أثناء البناء
          NODE_OPTIONS="--no-warnings" npx eas-cli build -p android --profile preview --non-interactive
