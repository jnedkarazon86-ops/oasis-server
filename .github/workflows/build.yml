name: Oasis Final Build
on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          echo "legacy-peer-deps=true" > .npmrc
          
          # إعداد eas.json لضمان القناة وبيئة البناء الصحيحة
          echo '{
            "build": {
              "preview": {
                "android": {
                  "buildType": "apk",
                  "image": "latest"
                },
                "channel": "preview"
              }
            }
          }' > eas.json

          # إعداد app.json لضمان تعريفات الأندرويد الأساسية
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp",
                "compileSdkVersion": 34,
                "targetSdkVersion": 34,
                "buildToolsVersion": "34.0.0"
              },
              "extra": {
                "eas": { "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53" }
              }
            }
          }' > app.json
          
          npm pkg delete type
          npm install --legacy-peer-deps
          
          # [تصحيح خطأ الصورة 1000079246]: الحقن بطريقة متوافقة مع Gradle 8.3
          # سنستخدم علامة (=) ونضع الإعدادات داخل بلوك android مباشرة لضمان التعرف عليها
          find node_modules -name "*.gradle" -exec sed -i '/android {/a \    namespace = "com.oasis.app"\n    compileSdkVersion = 34\n    targetSdkVersion = 34' {} + || true

          # تصحيح مراجع Kotlin لضمان عدم تعارض المكونات
          find node_modules -name "*.gradle" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "*.kts" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          
          # إصلاح تعارضات ZegoCloud والاتصال
          find node_modules -name "AndroidManifest.xml" -exec sed -i 's/package="com.zegouikitrebuiltcallrn"/ /g' {} + || true

      - name: 🚀 Build APK (Final Fix & Clear Cache)
        run: |
          cd mobile
          # إجبار النظام على بناء نظيف تماماً لمسح أخطاء المحاولات السابقة
          npx eas-cli build --platform android --profile preview --non-interactive --clear-cache
