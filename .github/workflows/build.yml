name: Oasis Auto Build APK
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Final Dependency Prep
        run: |
          cd mobile
          # إزالة نوع module لضمان توافق expo config
          npm pkg delete type
          npm install --legacy-peer-deps
          # إصلاح ZegoCloud
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK
        run: |
          cd mobile
          # استخدام محرك البناء في وضع التوافق
          EXPO_USE_METRO_REQUIRE=1 npx eas-cli build -p android --profile preview --non-interactive
