name: Oasis Final Build
on:
  push:
    branches:
      - main
      - master
  workflow_dispatch: # يسمح بتشغيل البناء يدوياً من تبويب Actions في حال لم يبدأ تلقائياً

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Match Credentials & Prep
        run: |
          cd mobile
          # حل تعارضات npm لضمان تثبيت حزم Zego و Expo بنجاح
          echo "legacy-peer-deps=true" > .npmrc
          
          # [حل جذري لخطأ compileSdkVersion الموضح في السجلات]
          echo '{
            "expo": {
              "name": "Oasis",
              "slug": "oasis-app",
              "version": "1.0.0",
              "owner": "aboahj",
              "android": {
                "package": "com.aboahj.oasisapp",
                "compileSdkVersion": 34,
                "targetSdkVersion": 34,
                "buildToolsVersion": "34.0.0",
                "permissions": ["CAMERA", "RECORD_AUDIO", "POST_NOTIFICATIONS"]
              },
              "extra": {
                "eas": { "projectId": "742d678c-90bd-4cf1-b261-604691b3cf53" }
              }
            }
          }' > app.json
          
          npm pkg delete type
          
          # تثبيت المكتبات اللازمة ومعالجة أخطاء المراجع
          npm install --legacy-peer-deps
          npm install @expo/config-plugins @expo/prebuild-config --save-dev --legacy-peer-deps
          
          # [الإجراء التصحيحي لخروج الأمر]: توليد ملفات الأندرويد يدوياً قبل التجميع
          npx expo prebuild --platform android --no-install
          
          # حقن الإعدادات المفقودة (SDK 34 و Namespace) مباشرة لمنع انهيار التقييم
          find android -name "build.gradle" -exec sed -i 's/compileSdkVersion .*/compileSdkVersion 34/g' {} + || true
          find android -name "build.gradle" -exec sed -i 's/targetSdkVersion .*/targetSdkVersion 34/g' {} + || true
          find android -name "build.gradle" -exec sed -i '/android {/a \    namespace "com.oasis.app"' {} + || true

          # تصحيح مراجع Kotlin (extensions) وإصلاح تعارضات ZegoCloud
          find node_modules -name "*.gradle" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          find node_modules -name "*.gradle" -exec sed -i 's/extra\[/project.ext\[/g' {} + || true
          find node_modules -name "*.kts" -exec sed -i 's/extensions.extraProperties/project.ext/g' {} + || true
          
          find node_modules -name "AndroidManifest.xml" -exec sed -i 's/package="com.zegouikitrebuiltcallrn"/ /g' {} + || true
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK (Final Fix & Clear Cache)
        run: |
          cd mobile
          export NPM_CONFIG_LEGACY_PEER_DEPS=true
          # مسح التخزين المؤقت لضمان عدم تكرار أخطاء الصور السابقة وبدء بناء نظيف
          npx eas-cli build --platform android --profile preview --non-interactive --clear-cache
