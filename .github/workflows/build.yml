name: Oasis Auto Build APK
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./mobile # تحديد مجلد العمل الرئيسي لحل جميع المشاكل

    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Install dependencies
        run: npm install --legacy-peer-deps

      - name: 🛠 Surgical Patch (Fix ZegoCloud Code)
        # هذا الأمر سيجد الملفات ويصلحها أينما كانت داخل المجلد
        run: |
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK
        run: npx eas-cli build -p android --profile preview --non-interactive
