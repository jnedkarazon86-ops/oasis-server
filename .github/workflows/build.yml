name: Oasis Auto Build APK
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🏗 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 🏗 Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 📦 Final Clean & Fix
        run: |
          cd mobile
          # مسح أي إعدادات تسبب خطأ الـ import
          npm pkg delete type
          # تحويل أي ملف config.js إلى cjs لضمان القبول
          find . -name "app.config.js" -exec mv {} {}.cjs \; || true
          npm install --legacy-peer-deps
          # إصلاح ZegoCloud الجراحي
          find node_modules -name "index.js" -path "*zegocloud*" -exec sed -i "s/import { typeof } from 'react-native';/import { } from 'react-native';/g" {} + || true

      - name: 🚀 Build APK
        run: |
          cd mobile
          npx eas-cli build -p android --profile preview --non-interactive
